using System;
using Project1;
using Game;
using Main;

namespace topUsers
{
    public class Top
    {
        public void Users()
        {
            DiceRoller data = new DiceRoller();
            Console.WriteLine($"Top users:\ndata.Roller(name) : data.Roller(rollTotal)");
        }
    }
}
